Today I will simulate download_firmware.py as a download automation that will monitor all activities, namely loading logger.py and performing a local hash after downloading with an online hash, namely loading verify_hash.py and finally verifying the signature to determine the authenticity of the file from the developer to the hacker, namely loading verify_signature.py

so when you see the contents of my repo structure starting from the firmware directory, keys and output. from my firmware I have v1 and v2 directories, v1 from developer and v2 from hacker simulation, and I put the root in the firmware directory because I have flask and docker connected with ipv4 so I have to match the same structure when I initiate the flask server and docker, as well as in the key directory that contains key-pair-1 from the developer with key-pair-2 from hacker simulation, as well as the Output directory that contains key_pair_1 from the developer with key_pair_2 which contains hacker simulation, in each key_pair_version has the same content such as hash_result.md from the hash in the firmware directory as v1 and signature.sig in the keys directory in key_pair_1 as developer and and for key_pair_2 comes from all hacker simulations

for flask and docker servers are the same and have similarities to take folders from their paths, for example if the firmware.bin file is in the firmware folder, as well as outputs that have signature.sig and hash_result,md even though from these three files I created a sub in the directory such as v1 and v2 in the same firmware in Outputs also have key_pair_1 and key_pair_2 which will not be published and only published directly from the directory without going into the sub directory

for 1 week I learned from the beginning how the automatic update mechanism is carried out by the OTA Server, then I created a download_firmware.py file that will run on the client side, then I created another logger.py that will monitor all activities on the client, so from here I have download_firmware.py that will import logger.py, then I created a verify_hash to hash the firmware.bin file that has been downloaded into a local hash by comparing the server hash, so download_firmware.py will import verify_hash to be able to run hash automation and I created a verify_signature.py file to be able to verify whether the authenticity of this file comes from a developer or a hacker by comparing the downloaded signature.sig with the public key that has been embedded with the IOT device, the mechanism is that 1 private key will produce 1 public key, namely the key pair so if the signature, sig is different from the developer, then the public key will look for elements in the public key, there should be several in the private key that are wrapped into signature.sig because this signature.sig to make it we need the same firmware private_key, this is the important role of the public key that can recognize the private key that has been created, so the results are valid and invalid, valid will be implemented if invalid then it fails to be implemented in the IOT device, and finally I did a simulation from dev and hacker to find out whether download_firmware.py which imports from logger.py, verify_hash.py and verify_signature.py will automate all processes, then the output I got I have put in the log folder in the log-verify-signature sub which has 2 sub, namely the valid sub from flask on the same device and different devices, as well as docker on the same device with on different devices. And finally there is a sub invalid that explains the 3 conditions, namely the first condition firmware_modification.log, namely changes to the firmware contents made by hackers and the second condition firmware_modification&hash_result.log, namely changes to the firmware contents with the server hash changed by hackers and the final condition is firmware_modification&hash_modification&signature_modificattion.log changes to the firmware contents with the server hash and signature changed by hackers.
